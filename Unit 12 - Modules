# https://www.openbookproject.net/thinkcs/python/english3e/
# https://docs.google.com/presentation/u/1/d/1zHIza5441BJS4xOFOTuE5hAD6UEVimJv/edit?usp=drive_web&ouid=106717267860750047546&rtpof=true

import random

""" 
Generate a list containing num random ints between lower_bound and upper_bound. upper_bound is an open bound. 
"""

def make_random_ints(num, lower_bound, upper_bound):
  # Create a random number generator
  rng = random.Random()  
  result = []
  for i in range(num):
    result.append(rng.randrange(lower_bound, upper_bound))
  return result


for i in range(10):
# Pick 5 random month numbers
  print(make_random_ints(5, 1, 13))  
